# Iowa corn yield model

County-level corn yield prediction from Sentinel-2 phenology.
Train 2017-2024, holdout 2025, forward prediction for 2026.

## Status
Pre-session-1. No data, no model, no baseline yet.

## Setup
    python -m venv .venv && source .venv/bin/activate
    pip install -e ".[dev]"
    cp .env.example .env   # add your NASS key

## Run
    (nothing yet)
